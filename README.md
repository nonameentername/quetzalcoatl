quetzalcoatl
============

Velocicode 2026 gamejam.

Installation
------------

1. Download the dependencies:

```bash
godot --headless -s package.gd install
godot --headless -s plug.gd install force
```

2. Import godot resources

```bash
godot --headless --import
```
